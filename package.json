{
  "name": "statistics.map",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx @11ty/eleventy --pathprefix=statistics-maps",
    "serve": "npx @11ty/eleventy --serve",
    "dataset": "node scripts/generate-dataset.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^2.0.0",
    "tablesort": "^5.6.0"
  }
}
